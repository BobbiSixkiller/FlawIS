overwrite: true
schema: "../core/schema.graphql"
documents: "**/*.{gql,graphql}"
generates:
  src/lib/graphql/generated/:
    preset: "client"
    presetConfig:
      fragmentMasking: false
    config:
      documentMode: "string"
      addTypename: true
    plugins: []
